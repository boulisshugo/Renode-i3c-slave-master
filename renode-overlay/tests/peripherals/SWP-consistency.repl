// A CLF with an echoing UICC: every I-frame payload comes straight back, so the whole
// framing / CRC / sequencing chain can be checked byte-for-byte.
swp: SWP.SimpleSWPController @ sysbus

uicc: Mocks.EchoSWPDevice @ swp 0
